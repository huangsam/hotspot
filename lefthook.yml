# https://lefthook.dev/configuration/output.html
output:
  - failure
  - summary

# https://lefthook.dev/configuration/Hook.html
pre-commit:
  parallel: true
  commands:
    go-lint:
      glob: "*.go"
      run: make build
    go-test:
      glob: "*.go"
      run: make test
